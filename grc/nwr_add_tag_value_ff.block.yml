# auto-generated by grc.converter

id: nwr_add_tag_value_ff
label: Add Tag Value
category: '[nwr]'

parameters:
-   id: tagname
    label: Tag Name
    dtype: string
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import nwr
    make: nwr.add_tag_value_ff(${tagname}, ${vlen})

file_format: 1
