# auto-generated by grc.converter

id: nwr_burst_detect_and_tag
label: Burst Detect and Tag
category: '[nwr]'

parameters:
-   id: type
    label: IO Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char]
    hide: part
-   id: sample_rate
    label: Sample Rate
    dtype: float
    default: samp_rate
    hide: part
-   id: thresh_db
    label: Threshold (dB)
    dtype: float
    default: '0.0'
-   id: init_floor_est
    label: Initial Floor Estimate
    dtype: float
    default: 100e-6
-   id: declare_dur
    label: Declaration Duration (s)
    dtype: float
    default: 1.920e-3*8.0*16.0
-   id: max_dur
    label: Maximum Duration (s)
    dtype: float
    default: 1.920e-3*8.0*268.0
-   id: sob_amp_frac
    label: SOB Amplitude Fraction
    dtype: float
    default: '0.2'
-   id: sob_key
    label: Start Tag Name
    dtype: string
    default: '"rx_sob"'
-   id: eob_key
    label: End Tag Name
    dtype: string
    default: '"rx_eob"'
-   id: eob_delay
    label: EOB Delay
    dtype: float
    default: '0.0'
-   id: guard_interval
    label: Burst Guard Interval
    dtype: float
    default: '0.9'

inputs:
-   domain: stream
    dtype: ${ type }
-   label: mag
    domain: stream
    dtype: float
-   domain: message
    id: floor_msg
    optional: true

outputs:
-   domain: stream
    dtype: ${ type }

templates:
    imports: import nwr
    make: nwr.burst_detect_and_tag(${type.size}, ${sample_rate}, ${thresh_db}, ${init_floor_est},
        ${declare_dur}, ${max_dur}, ${sob_amp_frac}, ${sob_key}, ${eob_key}, ${eob_delay},
        ${guard_interval})

file_format: 1
