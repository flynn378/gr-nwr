# auto-generated by grc.converter

id: nwr_correction_estimator_ff
label: Correction Estimator
category: '[nwr]'

parameters:
-   id: inspection_length
    label: Inspection Length
    dtype: int
-   id: inspection_offset
    label: Inspection Offset
    dtype: int
    default: '0'
-   id: peak_ref
    label: Expected Peak Level
    dtype: float
    default: '1.0'
-   id: trough_ref
    label: Expected Trough Level
    dtype: float
    default: '-1.0'
-   id: offset_corr_key
    label: Offset Corr Tag Name
    dtype: string
    default: '"offset_corr"'
    hide: part
-   id: scale_corr_key
    label: Scale Corr Tag Name
    dtype: string
    default: '"scale_corr"'
    hide: part
-   id: scale_eob_zero
    label: Scale Corr at End
    dtype: bool
    default: 'False'
    options: ['False', 'True']
    option_labels: ['1.0', '0.0']
-   id: timing_win_start
    label: Timing Est. Window Start
    dtype: int
    default: '-1'
    hide: ${ ('part' if timing_win_start == -1 else 'none') }
-   id: timing_win_end
    label: Timing Est. Window End
    dtype: int
    default: '-1'
    hide: ${ ('part' if timing_win_end == -1 else 'none') }
-   id: time_est_key
    label: Timing Estimate Tag Name
    dtype: string
    default: '"time_est"'
    hide: part
-   id: clock_est_key
    label: Clock Estimate Tag Name
    dtype: string
    default: '"clock_est"'
    hide: part
-   id: sob_key
    label: Start Tag Name
    dtype: string
    default: '"rx_sob"'
    hide: part
-   id: eob_key
    label: End Tag Name
    dtype: string
    default: '"rx_eob"'
    hide: part

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import nwr
    make: nwr.correction_estimator_ff(${inspection_length}, ${inspection_offset},
        ${peak_ref}, ${trough_ref}, ${offset_corr_key}, ${scale_corr_key}, ${scale_eob_zero},
        ${timing_win_start}, ${timing_win_end}, ${time_est_key}, ${clock_est_key},
        ${sob_key}, ${eob_key})

file_format: 1
