# auto-generated by grc.converter

id: nwr_lms_da_equalizer_ff
label: LMS Data-Aided Equalizer
category: '[nwr]'

parameters:
-   id: training_samples
    label: Training samples
    dtype: float_vector
-   id: sync_tag
    label: Sync tag name
    dtype: string
-   id: num_taps
    label: Equalizer filter length
    dtype: int
-   id: mu
    label: LMS DA update step gain
    dtype: float

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import nwr
    make: nwr.lms_da_equalizer_ff(${training_samples}, ${sync_tag}, ${num_taps}, ${mu})
    callbacks:
    - set_gain(${mu})

file_format: 1
