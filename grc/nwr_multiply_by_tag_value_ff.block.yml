# auto-generated by grc.converter

id: nwr_multiply_by_tag_value_ff
label: Multiply by Tag Value
category: '[nwr]'

parameters:
-   id: tagname
    label: Tag Name
    dtype: string
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import nwr
    make: nwr.multiply_by_tag_value_ff(${tagname}, ${vlen})

file_format: 1
