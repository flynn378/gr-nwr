# auto-generated by grc.converter

id: nwr_same_burst_decoder
label: SAME Burst Decoder
category: '[nwr]'

parameters:
-   id: sob_key
    label: Start Tag Name
    dtype: string
    default: '"rx_sob"'
-   id: eob_key
    label: End Tag Name
    dtype: string
    default: '"rx_eob"'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: message
    id: printable
    optional: true
-   domain: message
    id: bytes
    optional: true
-   domain: message
    id: hard_bits
    optional: true
-   domain: message
    id: soft_bits
    optional: true

templates:
    imports: import nwr
    make: nwr.same_burst_decoder(${sob_key}, ${eob_key})

file_format: 1
